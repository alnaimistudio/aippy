e=Math.min(1.25,c.gain*volume);if(AC.createStereoPanner){const p=AC.createStereoPanner();p.pan.value=Math.max(-1,Math.min(1,pan));src.connect(p).connect(g)}else src.connect(g);g.connect(master);src.start(AC.currentTime+delay,c.start,c.dur)}
function startNature(){natureStarted=true;const n=AC.createBufferSource(),g=AC.createGain(),f=AC.createBiquadFilter(),buf=AC.createBuffer(1,AC.sampleRate*3,AC.sampleRate),arr=buf.getChannelData(0);for(let i=0;i<arr.length;i++)arr[i]=Math.random()*2-1;n.buffer=buf;n.loop=true;f.type='lowpass';f.frequency.value=780;g.gain.value=.018;n.connect(f).connect(g).connect(master);n.start();const bird=()=>{if(!natureStarted)return;const o=AC.createOscillator(),gg=AC.createGain(),t=AC.currentTime;o.type='sine';o.frequency.setValueAtTime(1900+Math.random()*900,t);o.frequency.exponentialRampToValueAtTime(2900+Math.random()*700,t+.07);o.frequency.exponentialRampToValueAtTime(1600+Math.random()*400,t+.16);gg.gain.setValueAtTime(.0001,t);gg.gain.exponentialRampToValueAtTime(.015,t+.025);gg.gain.exponentialRampToValueAtTime(.0001,t+.18);o.connect(gg).connect(master);o.start(t);o.stop(t+.2);setTimeout(bird,2200+Math.random()*4600)};setTimeout(bird,1300)}
function deerSpatial(d){const local=d.root.position.clone().sub(C.position).applyQuaternion(C.quaternion.clone().invert()),dist=Math.max(1,local.length());return{pan:Math.max(-1,Math.min(1,local.x/Math.max(1,Math.hypot(local.x,local.z)))),volume:Math.max(.15,Math.min(.65,1-dist/90))}}
function deerVoice(d,type='snort',extra=1,delay=0){if(!d?.root?.visible)return;const s=deerSpatial(d);playCue(type,{volume:s.volume*extra,pan:s.pan,rate:.95+Math.random()*.1,delay})}

const MODEL_URL='https://cdn.jsdelivr.net/gh/Quaternius/TestGltfAssets@master/Deer/Deer.glb';
const loader=new GLTFLoader();let deerSource=null,modelReady=false;
const RIG_NAMES=['fl1.l','fl2.l','fl3.l','fl1.r','fl2.r','fl3.r','rl1.l','rl2.l','rl3.l','rl1.r','rl2.r','rl3.r','neck1','neck2','headctrl','thorax','pelvis'];
const modelPromise=loader.loadAsync(MODEL_URL).then(g=>{deerSource=g.scene;deerSource.traverse(o=>{if(o.isMesh){o.castShadow=false;o.receiveShadow=true}});modelReady=true;return deerSource});
const DEER=[],MIN_R=26,MAX_R=66,BAG_CAP=5;
function normalizeModel(m){m.rotation.y=Math.PI/2;m.updateMatrixWorld(true);let b=new THREE.Box3().setFromObject(m),s=new THREE.Vector3();b.getSize(s);m.scale.setScalar(2.65/Math.max(.001,s.y));m.updateMatrixWorld(true);b=new THREE.Box3().setFromObject(m);const c=new THREE.Vector3();b.getCenter(c);m.position.x-=c.x;m.position.z-=c.z;m.position.y-=b.min.y;m.updateMatrixWorld(true)}
function captureRig(m){const rig={},base={};m.traverse(o=>{const n=(o.name||'').toLowerCase();if(RIG_NAMES.includes(n)){rig[n]=o;base[n]=o.rotation.clone()}});return{rig,base}}
function randomSpawn(){const a=Math.random()*Math.PI*2,r=31+Math.random()*27;return new THREE.Vector3(Math.cos(a)*r,0,Math.sin(a)*r)}
function spawnDeer(d){const p=randomSpawn();d.root.position.set(p.x,ty(p.x,p.z),p.z);d.root.rotation.set(0,Math.random()*Math.PI*2,0);d.visual.rotation.set(0,0,0);d.visual.position.set(0,0,0);d.hp=100;d.state='wander';d.t=Math.random()*4;d.dir=Math.atan2(-p.z,-p.x)+(Math.random()-.5)*1.9;d.root.rotation.y=-d.dir+Math.PI/2;d.root.visible=true;d.respawn=0;d.hitKick=0}
function createDeer(index){const root=new THREE.Group(),visual=new THREE.Group();root.add(visual);S.add(root);const model=cloneSkeleton(deerSource);normalizeModel(model);visual.add(model);const {rig,base}=captureRig(model);const d={index,root,visual,model,rig,base,hp:100,state:'wander',t:Math.random()*5,dir:Math.random()*6.28,speed:.72+Math.random()*.38,respawn:0,hitKick:0,phase:Math.random()*6.28};spawnDeer(d);return d}
function setBone(d,n,a,v){const b=d.rig[n],r=d.base[n];if(b&&r)b.rotation[a]=r[a]+v}
function animateRig(d,now,running,idle){const f=running?10.8:idle?1.25:4.2,A=running?.68:idle?.04:.23,p=now*.001*f+d.phase,a=Math.sin(p)*A,b=-a;setBone(d,'fl1.l','x',a);setBone(d,'fl1.r','x',b);setBone(d,'rl1.l','x',b);setBone(d,'rl1.r','x',a);setBone(d,'fl2.l','x',Math.max(0,-a)*(running?.55:.35));setBone(d,'fl2.r','x',Math.max(0,-b)*(running?.55:.35));setBone(d,'rl2.l','x',Math.max(0,-b)*(running?.72:.48));setBone(d,'rl2.r','x',Math.max(0,-a)*(running?.72:.48));setBone(d,'fl3.l','x',-Math.max(0,a)*.28);setBone(d,'fl3.r','x',-Math.max(0,b)*.28);setBone(d,'rl3.l','x',-Math.max(0,b)*.32);setBone(d,'rl3.r','x',-Math.max(0,a)*.32);setBone(d,'neck1','z',Math.sin(p*.45)*(running?.055:idle?.035:.025));setBone(d,'headctrl','z',Math.sin(p*.32)*(idle?.12:running?.03:.055));setBone(d,'thorax','z',Math.sin(p*2)*(running?.035:.012));setBone(d,'pelvis','z',-Math.sin(p*2)*(running?.03:.01));d.visual.position.y=Math.abs(Math.sin(p))*(running?.055:idle?.003:.011);d.visual.rotation.x=-d.hitKick+Math.sin(p*2)*(running?.018:.006)}
function flee(d){if(!d.root.visible||d.state==='dead')return;d.state='run';d.t=0;d.dir=Math.atan2(d.root.position.z-C.position.z,d.root.position.x-C.position.x)+(Math.random()-.5)*.38;d.hitKick=.12}
function animateDeer(d,dt,now){if(!d.root.visible){d.respawn-=dt;if(d.respawn<=0)spawnDeer(d);return}d.t+=dt;d.hitKick=Math.max(0,d.hitKick-dt*1.7);if(d.state==='dead'){d.visual.rotation.z=THREE.MathUtils.lerp(d.visual.rotation.z,Math.PI*.47,1-Math.exp(-4.5*dt));d.respawn-=dt;if(d.respawn<=0){d.root.visible=false;d.respawn=1.5}return}const running=d.state==='run';let speed=running?5.8:d.speed;if(d.state==='wander'&&Math.random()<.0045)d.dir+=(Math.random()-.5)*1.0;const radius=Math.hypot(d.root.position.x,d.root.position.z);if(radius<MIN_R+2){d.dir=Math.atan2(d.root.position.z,d.root.position.x)+(Math.random()-.5)*.25;speed=Math.max(speed,1.5)}else if(radius>MAX_R){d.dir=Math.atan2(-d.root.position.z,-d.root.position.x)+(Math.random()-.5)*.5}d.root.position.x+=Math.cos(d.dir)*speed*dt;d.root.position.z+=Math.sin(d.dir)*speed*dt;d.root.position.y=ty(d.root.position.x,d.root.position.z);d.root.rotation.y=-d.dir+Math.PI/2;if(running&&d.t>4.2){d.state='wander';d.t=0}anim